reply to ...

  <%= render @comment.parent if @comment.parent %>


<% unless current_user.is_a? GuestUser %>
  <%= form_for @comment, url: '#' do |f| %>
  <%= f.hidden_field :parent_id %>
    <div class="form-group">
      <%= f.text_area :content, class: 'form-control', placeholder: "Reply to..." %>
    </div>


    <%= link_to 'Reply', new_comment_path(@blog_id, :parent_id => @comment) %>
    <!--<%= f.submit 'Post Comment', class: 'btn btn-primary shadow-none' %>-->
  <% end %>
<% end %>


all replies down below
